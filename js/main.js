(function(){
  "use strict";

// I want the game to automagically choose a random number upon initial page load, so this is an iife
(function(){
  commonWords[Math.floor(Math.random() * commonWords.length)];

}());











}());
